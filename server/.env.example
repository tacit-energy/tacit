# Copy to .env and fill in. Generate the token with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Optional overrides
# ENERGYOPS_PORT=3461
# Path to the dataset directory (used from P1 onward)
# DATA_DIR=../../energyops_copilot_sample_dataset
